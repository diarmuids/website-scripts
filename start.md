# Distribution repository startup

Read `AGENTS.md` in this directory. Client projects, shared standards, activation
commands, and publishing instructions now live in:

```text
C:\Users\diarm\Desktop\Webflow
```

Open `C:\Users\diarm\Desktop\Webflow\Webflow.code-workspace` for an overview,
or activate and open a client with:

```powershell
C:\Users\diarm\Desktop\Webflow\shared\tools\activate-project.ps1 <slug>
```
